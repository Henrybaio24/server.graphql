type Query {
  me: User!
  user(id: ID!): user
  users: [User]!
}

type Mutation {
    signup(name: String!, email: String!, password: String!): User
}

type User {
  id: ID!
  name: String!
  email:
}
